<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowBackground">@drawable/window_background</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="AppTheme">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="ResultActivity_Theme" parent="AppTheme.NoActionBar">
        <item name="android:windowBackground">@drawable/pink_window_background</item>
    </style>

    <style name="ToolBarStyle" parent="Widget.AppCompat.Toolbar">
        <item name="android:homeAsUpIndicator">@drawable/ic_action_back</item>
    </style>

    <style name="ToolBarStyle.TitleTextAppearance" parent="TextAppearance.AppCompat">
        <item name="android:textColor">#fff</item>
        <item name="android:textSize">15sp</item>
    </style>

    <style name="ToolBarStyle.SubTitleTextAppearance" parent="TextAppearance.AppCompat">
        <item name="android:textColor">#9e9e9e</item>
        <item name="android:textSize">12sp</item>
    </style>

</resources>
